# Contributing to NodeArmor

Contributions to the open client (enforce.py, auth.py, config schema, docs) are welcome.

1. Fork the repository
2. Create a branch: `git checkout -b feature/your-feature`
3. Open a pull request

The policy engine is private — not in scope for external contributions.
